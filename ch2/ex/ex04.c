#include <stdio.h>

int main(void)
{
	int a, b, c, d, e;
	float h, i, j, k, l;

	printf("int: %d, %d, %d, %d, %d\n", a, b, c, d, e);
	printf("float: %f, %f, %f, %f, %f\n", h, i, j, k, l);

	return 0;
}
